name: build-simplehub

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout current repo
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build backend
        working-directory: server
        run: |
          npm install --no-audit --no-fund
          npx prisma generate

      - name: Build frontend
        working-directory: web
        run: |
          npm install --no-audit --no-fund
          npm run build

      - name: Pack
        run: |
          rm -rf release
          mkdir -p release/SimpleHub
          cp -r server release/SimpleHub/
          mkdir -p release/SimpleHub/server/web
          rm -rf release/SimpleHub/server/web/dist
          cp -r web/dist release/SimpleHub/server/web/

          cat > release/SimpleHub/run-simplehub.sh <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail
          cd "$HOME/SimpleHub/server"
          export PORT="${PORT:-8100}"
          export HOST="${HOST:-${IP:-::}}"
          export NODE_ENV="${NODE_ENV:-production}"
          export DATABASE_URL="${DATABASE_URL:-file:./data/db.sqlite}"
          exec npm start
          EOF

          chmod +x release/SimpleHub/run-simplehub.sh
          tar -C release -czf simplehub-prebuilt.tar.gz SimpleHub

      - name: Publish release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete latest-build -y || true
          git push origin :refs/tags/latest-build || true
          gh release create latest-build simplehub-prebuilt.tar.gz --title latest-build --notes "auto build"
